[phases.setup]
nixPkgs = ["python3", "nodejs", "git"]

[phases.install]
cmds = [
  "pip3 install -r requirements.txt",
  "npm install"
]

[phases.build]
cmds = [
  "echo 'Build completed'"
]

[start]
cmd = "python3 railway_startup.py"
