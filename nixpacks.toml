[phases.setup]
nixPkgs = ["python3", "nodejs", "npm", "git"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install"
]

[phases.build]
cmds = [
  "echo 'Build completed'"
]

[start]
cmd = "python3 railway_startup.py"
